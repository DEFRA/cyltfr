/*
 * Currently let's just create a single bundle that includes the `map` page js.
 * Down the line we can include more pages here or alternatively, separate out into differeent bundles.
 */
require('./map')
